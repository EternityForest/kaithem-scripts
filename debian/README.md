## KaithemAutomation Debian scripts

A lot of helper scripts for configuration have been removed because
underlying OS changes kept breaking stuff.

These scripts are now mostly for configuring the host for use in docker mode,
running Kaithem on bare metal for a real deployment is not longer suggested.